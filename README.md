# Unindenter

Unindenter is a simple website that allows you to unindent text.

### Options

**Default** - Removes all indentation from the first line, removes the same amount from all other lines.  
**All** - Removes all indentation from all lines.  
**Custom** - Removes indentation based on user input.
